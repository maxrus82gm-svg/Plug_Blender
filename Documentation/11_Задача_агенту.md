# Задача агенту — Plug_Blender

Рабочий файл текущего цикла. Канонический порядок постановки, исполнения, отчёта и review: START (`Documentation/12_Старт_агента.md`). Краткая история: `Documentation/20_SplitDoc/TASK_HISTORY.md`.

## БЛОК 1 — ТЕКУЩАЯ ЗАДАЧА

TASK 013 — Agent Feedback Window + bounded diagnostic log

**STATUS:** IN PROGRESS. AGENT REPORT ещё не формировать; после реализации и automated/runtime checks показать UI пользователю для визуальной проверки.

Основной scope исходной постановки сохраняется: отдельный читаемый `AGENT FEEDBACK LOG` в Blender N-panel, bounded runtime history, вторичный bounded `.runtime/agent_feedback.jsonl` только после подтверждённой доставки, context economy, инструкции Codex Agent, setup, пропорциональные tests и real GPT-5.6 Sol MEDIUM check. Не менять transport architecture, не добавлять heavy geometry/screenshots/arbitrary Python/public history tool, не читать `_research`, не выполнять изменяющие Git-действия.

Дополнение пользователя до AGENT REPORT:

- последовательные notes с точным совпадением `status + summary + details` объединять в один cluster;
- cluster хранит `repeat_count`, `first_time`, `last_time`; появление другого feedback завершает cluster;
- persistent JSONL также агрегирует только последовательные точные дубли;
- карточка по умолчанию показывает status/time/count, полный summary и небольшой details preview; полный details раскрывается локально, без Scene/`.blend` state;
- правило stop-on-problem: одна проблема → максимум одна полезная read-only диагностика → один WARNING/BLOCKED → STOP зависимой ветки;
- test: 100 одинаковых последовательных WARNING дают один visual/persistent cluster с `repeat_count=100`; после другого сообщения тот же WARNING создаёт новый cluster.

## БЛОК 2 — ПОСЛЕДНИЙ ОТЧЁТ АГЕНТА

AGENT REPORT будет сформирован после пользовательской визуальной проверки.

## БЛОК 3 — REVIEW / STATUS

IN PROGRESS — AWAITING USER VISUAL CHECK AFTER IMPLEMENTATION.
