# Текущее состояние Plug_Blender

Срез после реализации TASK 005, 2026-09-05. TASK 003 принята после ручного end-to-end теста; TASK 004 синхронизирована как PASS по прямому указанию пользователя. Результат TASK 005 ожидает review.

## Проверенные факты репозитория

- Репозиторий: `maxrus82gm-svg/Plug_Blender`; основная и текущая локальная ветка: `main`.
- До TASK 001 репозиторий содержал только перенесённую документационную систему другого проекта и настройки её Obsidian vault. Рабочее дерево перед изменениями было чистым.
- Система адаптирована под Plug_Blender: активная база — `Documentation/`, корневая точка входа — `AGENTS.md`.
- Сохранены универсальные механизмы task-loop, SplitDoc, encoding и Git/review; предметные правила прежнего проекта исключены. Старый каталог документации удалён после проверки миграции.
- В TASK 001 пять существующих файлов `Documentation/.obsidian/` перенесены без изменения содержимого. Последующие пользовательские изменения настроек сохранены. Это настройки просмотра документации, а не архитектура продукта.
- TASK 002 синхронизирована как PASS по прямому указанию TASK 003. Краткие итоги и основания статусов находятся в `Documentation/20_SplitDoc/TASK_HISTORY.md`.
- Граф сохраняет структуру hub → документы. По уточнению пользователя SplitDoc INDEX связан с его новым `Documentation/Codex_Решение_Проблем.md`; Project_Docs INDEX дополнен инструкцией установки. Layout и цвета агент не менял.
- В START закреплён переносимый протокол «Большой блок + Малый блок».

## Реализация

Создан минимальный Astro Modeler: устанавливаемый add-on в `Plugins/AstroModeler/astro_modeler/`, внешний STDIO MCP server в `MCP/` и проверки в `Tests/`. Доступны два public tools: `create_cube()` с прежним контрактом и read-only `get_selected_context()` без аргументов.

Selected Context v1 возвращает name/type active object, выбранные объекты с мировым transform 3 × 4, mode и 3D Cursor с позицией/quaternion. Матрица компактно сохраняет pivot, local axes, scale, parenting/shear/reflection без дублирующих представлений. Полный контракт — в `Documentation/04_Архитектура.md`. Геометрия Mesh не читается и не сериализуется.

Blender явно регистрирует одну сессию кнопкой Start Integration. Локальный socket обслуживается неблокирующим таймером Blender; `bpy` не вызывается из фонового Python thread. Технические решения прототипа записаны в `Documentation/02_Решения.md` и `Documentation/04_Архитектура.md` и не объявлены production architecture.

## Подтверждённый runtime

- Среда: Windows, Blender 5.0.1 / встроенный Python 3.11.13; внешний Python 3.13.8; Codex CLI 0.153.3; официальный MCP SDK 1.29.1 в проектной `.venv/`.
- Установленный Codex app-server обнаружил ровно `create_cube` и выполнил два настоящих MCP-вызова в явно подключённый GUI Blender (`background=false`). Созданы `Cube.001` и `Cube.002`; сцена подтверждает 8 вершин, 6 граней и размеры 2 × 2 × 2 для каждого. Blender отвечал после повторного вызова.
- Сохранённый `.blend` независимо прочитан Blender и сопоставлен с ответами Codex. ZIP установлен и включён в изолированном каталоге; stop/restart/disable, отключение при загрузке файла и отказ в Edit Mode проверены.
- Восемь socket/protocol tests и SDK STDIO smoke пройдены. Результаты и воспроизведение: `Documentation/21_Project_Docs/ASTRO_MODELER_SETUP.md`.

Первоначальный тест использовал временный app-server context без модельного turn; тестовая GUI session после сохранения результата закрыта агентом. Затем пользователь установил Astro Modeler в рабочий Blender, запустил Start Integration и попросил в обычном текстовом диалоге Codex создать куб. Через MCP / Astro Modeler в текущей сцене создан `Cube`; результат подтверждён пользователем и его screenshot. TASK 003 синхронизирована как PASS по прямому указанию пользователя. Это подтверждение рабочего сценария не означает проверку сохранённой MCP-конфигурации Codex; агент её не менял.

TASK 005 дополнительно проверена в отдельном GUI Blender 5.0.1: пустое/одно/несколько выделенных объектов, active вне selection, MESH/EMPTY/LIGHT, Edit Mode, parented transform, нулевой масштаб, 3D Cursor с поворотом и вне origin, 100 выбранных объектов. Для Mesh с 8 и 1 000 000 вершин JSON structured result полностью совпал — 589 байт в сериализации теста, без MCP envelope. Проверены 20 чтений без изменения контекста в основном потоке; установленный Codex app-server прочитал context дважды, Create Cube создал ещё два тестовых объекта.

Пройдены 12 unit/socket tests, SDK smoke, compileall, pip check и установка актуального ZIP в изолированном Blender. Тестовая сессия закрыта; исходная пользовательская сессия не изменялась. Артефакты — `.runtime/selected-result.json`, `.runtime/codex-selected-result.json`, `.runtime/selected-context-smoke.blend`.

Полный Scene dump, изменение transforms, произвольный Python, screenshots/feedback, skills, multi-session routing и аналитика Mesh не реализованы. Концепция (`Documentation/21_Project_Docs/PROJECT_CONCEPT.md`) по-прежнему включает будущие возможности, не подтверждённые этим прототипом.

В TASK 004 расширена только карта направлений `Documentation/06_Идеи.md`. Существующие идеи сохранены; новые не являются реализацией, утверждённой архитектурой или обязательствами roadmap.

В TASK 005 туда кратко добавлен принцип «тяжёлая геометрия остаётся в Blender»: будущий анализ выполняется локально, Astra получает компактную сводку. Сам аналитический слой не реализован.

## Следующий шаг

Review TASK 005. Для ручного теста в рабочей сессии требуется переустановить пересобранный `dist/astro_modeler-0.1.0.zip`, перезапустить Blender и нажать Start Integration; затем спросить Codex о текущем выделении. Способ запуска сохранён, текущая пользовательская установка агентом не обновлялась. Новый tool проверен через SDK и установленный Codex app-server в тестовом Blender, а не через обычный диалог в рабочей сессии. Следующая TASK не запущена; очередь — `Documentation/05_Очередь_задач.md`.
