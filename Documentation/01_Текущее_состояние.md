# Текущее состояние Plug_Blender

Срез после реализации и runtime-проверки TASK 003, 2026-09-05. Результат ожидает review; агент не объявляет TASK принятой.

## Проверенные факты репозитория

- Репозиторий: `maxrus82gm-svg/Plug_Blender`; основная и текущая локальная ветка: `main`.
- До TASK 001 репозиторий содержал только перенесённую документационную систему другого проекта и настройки её Obsidian vault. Рабочее дерево перед изменениями было чистым.
- Система адаптирована под Plug_Blender: активная база — `Documentation/`, корневая точка входа — `AGENTS.md`.
- Сохранены универсальные механизмы task-loop, SplitDoc, encoding и Git/review; предметные правила прежнего проекта исключены. Старый каталог документации удалён после проверки миграции.
- В TASK 001 пять существующих файлов `Documentation/.obsidian/` перенесены без изменения содержимого. Последующие пользовательские изменения настроек сохранены. Это настройки просмотра документации, а не архитектура продукта.
- TASK 002 синхронизирована как PASS по прямому указанию TASK 003. Краткие итоги и основания статусов находятся в `Documentation/20_SplitDoc/TASK_HISTORY.md`.
- Граф сохраняет структуру hub → документы. По уточнению пользователя SplitDoc INDEX связан с его новым `Documentation/Codex_Решение_Проблем.md`; Project_Docs INDEX дополнен инструкцией установки. Layout и цвета агент не менял.
- В START закреплён переносимый протокол «Большой блок + Малый блок».

## Реализация

Создан минимальный Astro Modeler: устанавливаемый add-on в `Plugins/AstroModeler/astro_modeler/`, внешний STDIO MCP server в `MCP/` и проверки в `Tests/`. Единственный публичный tool — `create_cube()`; он создаёт куб 2 × 2 × 2 в начале координат текущей сцены и возвращает `success`, `object_name`, `message`.

Blender явно регистрирует одну сессию кнопкой Start Integration. Локальный socket обслуживается неблокирующим таймером Blender; `bpy` не вызывается из фонового Python thread. Технические решения прототипа записаны в `Documentation/02_Решения.md` и `Documentation/04_Архитектура.md` и не объявлены production architecture.

## Подтверждённый runtime

- Среда: Windows, Blender 5.0.1 / встроенный Python 3.11.13; внешний Python 3.13.8; Codex CLI 0.153.3; официальный MCP SDK 1.29.1 в проектной `.venv/`.
- Установленный Codex app-server обнаружил ровно `create_cube` и выполнил два настоящих MCP-вызова в явно подключённый GUI Blender (`background=false`). Созданы `Cube.001` и `Cube.002`; сцена подтверждает 8 вершин, 6 граней и размеры 2 × 2 × 2 для каждого. Blender отвечал после повторного вызова.
- Сохранённый `.blend` независимо прочитан Blender и сопоставлен с ответами Codex. ZIP установлен и включён в изолированном каталоге; stop/restart/disable, отключение при загрузке файла и отказ в Edit Mode проверены.
- Восемь socket/protocol tests и SDK STDIO smoke пройдены. Результаты и воспроизведение: `Documentation/21_Project_Docs/ASTRO_MODELER_SETUP.md`.

Тест использовал временный app-server context без модельного turn. Постоянная установка в рабочий Blender пользователя и вызов через обычный текстовый диалог текущего Codex app не проверялись; сохранённая MCP-конфигурация Codex не менялась. Для использования подготовлены ZIP и инструкции подключения. Исходная несохранённая пользовательская Blender session не изменялась; тестовая GUI session после сохранения результата закрыта агентом.

Список объектов, selection, transforms, произвольный Python, screenshots/feedback, skills и multi-session routing не реализованы. Концепция (`Documentation/21_Project_Docs/PROJECT_CONCEPT.md`) по-прежнему включает будущие возможности, не подтверждённые этим прототипом.

## Следующий шаг

Review TASK 003. Предложение следующего технического шага находится в `Documentation/05_Очередь_задач.md`; оно не запущено. Ограничения и будущие вопросы — в `Documentation/03_Открытые_вопросы.md`.
